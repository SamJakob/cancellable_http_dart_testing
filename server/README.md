# Cancellable HTTP Dart Testing (Server)

A simple server that logs each event in the request and response
lifecycle from the Node.js http server.

## Endpoints

- `/` (and any other non-specified)
  - immediately prints "Hello, world!"
